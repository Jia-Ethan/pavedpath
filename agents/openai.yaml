interface:
  display_name: "PavedPath"
  short_description: "Find proven paths for non-code problems from the open internet."
  default_prompt: "Use PavedPath to frame my non-code problem, use Agent Reach first when live public-internet evidence is needed and available, compare the strongest practical paths, and answer with executable steps at the right depth."
